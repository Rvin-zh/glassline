{
  "name": "folio",
  "description": "Folio is Glassline's external resume layer. It builds an ATS-safe, tailored resume outside the interview overlay.",
  "private": true,
  "engines": { "node": ">=20" },
  "scripts": { "test": "node --test test/*.test.js" },
  "dependencies": {
    "mammoth": "^1.9.0",
    "pdf-parse": "^1.1.1",
    "pdfkit": "^0.16.0"
  }
}
